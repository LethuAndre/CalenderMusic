app.get('/home', (req, res) => {
  // If we don't have tokens, send them to login immediately
  if (!app.locals.tokens) {
    return res.redirect('/login');
  }
  res.sendFile(__dirname + '/public/index.html');
});